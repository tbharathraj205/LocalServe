# LocalServe
QuickShare Server is a desktop application built with Python and PyQt6 that allows users to instantly host and share any local folder over a simple HTTP file server — without using command-line commands.
